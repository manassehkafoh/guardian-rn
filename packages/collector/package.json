{
  "name": "@guardian/collector",
  "version": "1.0.0",
  "private": true,
  "description": "Stateless ingest service: mTLS auth, HMAC verification, ECS validation, PII redaction, fan-out to Logstash/S3/Kafka",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "test": "jest --passWithNoTests",
    "seed": "tsx src/seed/index.ts"
  },
  "dependencies": {
    "fastify": "^5.8.5",
    "ajv": "^8.12.0",
    "ioredis": "^5.3.0",
    "pino": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "jest": "^29.0.0"
  }
}
